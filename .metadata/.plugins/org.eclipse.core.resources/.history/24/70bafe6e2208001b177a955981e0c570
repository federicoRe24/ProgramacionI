#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include "utn.h"
#include "alumno.h"
#include "calculosArray.h"

int main(void)
{
	/*int op;
	Alumno arrayAlumnos[5];
	int indiceVacio;

	7) Realizar un programa con un menu de dos opciones:
	    a) Alta de alumno. -NO PEDIR INDICE-
	    b) Imprimir lista alumnos

	alumno_initArray(arrayAlumnos,QTY_ALUMNOS);
	do
	{
		utn_getInt("\n1-Alta\n2-Mostrar\n3-Salir","\nError",&op,2,3,1);
		switch(op)
		{
			case 1:
				if(alumno_devolverVacio(arrayAlumnos, QTY_ALUMNOS, &indiceVacio) == 0)
				{
					alumno_alta(arrayAlumnos,QTY_ALUMNOS,indiceVacio);
				}
				break;
			case 2:
				alumno_imprimir(arrayAlumnos,QTY_ALUMNOS);
				break;
			case 3:
				op = 8;
				break;

		}
	}while(op != 8); */

/*	Alumno alumnos[5];
	int indiceItemVacio;

	if(alumno_initArray(alumnos, 5) == 0)
	{
		if(alumno_devolverVacio(alumnos, 5, &indiceItemVacio) == 0)
		{
			printf("%d",indiceItemVacio);
		}
		else
		{
			printf("No hay vacios.");
		}
	}*/

	return EXIT_SUCCESS;
}




